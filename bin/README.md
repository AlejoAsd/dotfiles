# bin

Custom git subcommands and scripts, installed to `~/.local/bin/`.

## Scripts

- **git-release** - Interactive semver tagging tool. Run `git release -h` for usage.

## Install / Uninstall

```bash
./install.sh    # Symlinks scripts to ~/.local/bin/
./uninstall.sh  # Removes symlinks and restores backups
```
